# vaultlock
a script to encrypt and decrypt files using a password. it uses pbkdf2 to derive the key and fernet for encryption.
wrote this so i could safely lock some local files.

usage: `python vaultlock.py`
